import React from "react";
import { Meta } from "@storybook/react";

export default {
  title: "Components/NoteForm/Buttons/SubmitButton",
} as Meta;

export const Default = () => <button type="submit">Add Note</button>;
